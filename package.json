{
  "name": "pusher-zod",
  "version": "0.0.1",
  "description": "pusher with typesafety using zod to parse the data",
  "main": "./dist/index.js",
  "module": "./dist/index.mjs",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepublish": "tsc",
    "build": "tsup"
  },
  "author": "lucas-yon",
  "license": "ISC",
  "dependencies": {
    "pusher": "^5.2.0",
    "pusher-js": "^8.4.0-rc2"
  },
  "devDependencies": {
    "ts-node": "^10.9.2",
    "tsup": "^8.0.1",
    "zod": "^3.22.4",
    "typescript": "^5.3.3"
  }
}
